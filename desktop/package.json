{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "typecheck": "echo \"no TypeScript in desktop shell\" && exit 0",
    "lint": "echo \"no JS/TS to lint in desktop shell\" && exit 0",
    "format": "echo \"nothing to format\" && exit 0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.3"
  }
}
